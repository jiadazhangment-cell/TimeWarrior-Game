# 执行报告 · E2 视觉修复 + 风格统一批(2026-08-09)

- **唯一改动文件**:`src/scenes/ArenaScene.js`(留在 working tree,未提交)。`config/`、`tools/`、其他 `src/` 一律未碰。
- **自查**:`node --check`(ES module 形态)通过。
- **静态验证**:因禁用浏览器,我把 ArenaScene 的 import 替换成桩后在 Node 里**真跑了全部绘制路径**并做几何断言 —— **34 条断言全过**,脚本在
  `C:\Users\surpr\AppData\Local\Temp\claude\C--Users-surpr\2743c226-92eb-4124-91ba-2cb7b77bd3a5\scratchpad\`(`make-harness2.mjs` / `stubs.mjs` / `drive.mjs` / `verify.mjs` / `cost.mjs`)。
  它能证明:无未定义引用、无拼错的 Phaser 方法名、坐标/相位/密度算式正确。**它不能替代实机观感验收**(见 §7)。
- 处理范围:spec-style-unify 五项全做;判真 #10–#21、#23 逐条(#14 按指令只出方案不动手)。

---

## 1. 最优先项:前景视差漂移一族(#12 + #23)—— 第一个修

### #12 `_drawRegionFg` 前景管束恒定西移(med)
- **病根**:`setScrollFactor(1.09)` 只定"跟随速度",不定基准点。屏幕位置 = `worldX − scrollX·f`,整层恒定西移 `(f−1)·scrollX`;本章 scrollX 4300–6840 ⇒ 西移 **387~616 世界px ≈ 半屏**。后果实测:R-B 大厅(风扇/换电站/总控台整段)**一根前景柱都不剩**,而 R-B 那两组管子跑进 R-A 走廊里显示。
- **改法(防线级,不是单点)**:新增 `fgAnchor(px) = px + (f−1)·(px − 相机半宽)`,把绘制坐标反解成"相机正对 px 时管子正好落在 px"。
  - 与 zoom 无关(Phaser 先按 scrollFactor 折世界坐标、再过相机矩阵),断言实测:t=0.29 与 t=0.4 两组在相机对准时屏幕坐标恰为 480.000。
  - **整组只挪不缩**:锚点只作用在每组的基准 x 上,组内几何(两根管间距 40、管宽 30/19)不被 f 放大。
  - 将来加 1.2–1.5 的更近前景层直接复用 `fgAnchor`(设计文档计划的风扇井多层前景,偏移会放大到 0.2–0.5×scrollX = 1300–3400 世界px,必须走同一 helper)。
- **顺带修**:`setScrollFactor(1.09)` → `setScrollFactor(1.09, 1)`。单参会把 Y 一起设,R-B 走道 scrollY≈453 时底部渐隐上移 ~41px(管子在半空就淡没了),纵向本无视差意图。
- **落点校验**(修复后 fgSpots 的设计意图终于生效,即 exec-map D9 的微调):
  - R-A `[0.10, 0.86]` → 显示落点 4730 / 5718(避开地沟坑口 5080·5330、甲板检修口 5545–5705 ✓)
  - R-B `[0.29, 0.4]` → 显示落点 6439.4 / 6644(避开补给间门 6125、总控台 7350;6644 正好在风扇叶片西缘 6647 前 —— 与调研文"叶片从前景管道后面扫过"的设计意图一致,**请实机确认是否要再让开**)
- **文档欠账(需主控)**:`scene-fx-pipeline/SKILL.md:23` 与 `开发日志.md:50` 里"1.09 实测不漂/1.14 才漂"这句是**文档背了 bug**,应订正。我不改 skill 文件(超范围)。

### #23 前景管束平涂 + 暗边被空区吃掉(low,同一对象的第二个缺陷)
- **真因**(候选说对结论、说错理由):暗边色 `0x05070B` 与新区暗底 `0x05070A` 只差 1/255 —— 描边在黑空区里等于不存在,圆柱三段塌成两段,最靠前的一层读作"贴在镜头上的灰条"。
- **改法**:
  - 暗边提到 `0x11161d`(左)/`0x0e131a`(右),明显高于空区底色;
  - 管身改**横向四段渐变**(`fillGradientStyle`,项目内已有 6 处用例):`0x11161d→0x1b232e→0x3d4a5a(高光核)→0x232c38→0x0e131a`,段间 +0.6px 消发丝缝;
  - 卡箍升级成**法兰接头**:外框 + 双向渐变箍身 + 上受光棱 + 下沿投影 + 一圈螺栓(每 9px,带高光点);
  - **不加端帽**——`通顶 + 底部渐隐` 是踩坑后的定版(半空断头=悬空模型),加端帽等于把已修的 bug 改回去。
- 归并说明:#12 与 #23 是同一对象的两个独立缺陷,在同一函数一次改完。

---

## 2. spec-style-unify 五项

### ① 踏台×2 + 高台结构化
新增 `_drawPlatformRig(p, anchorX)` / `_gusset(...)` / `_drawRiserBody(p)`,三层 graphics **全场共用**(`this._rigG`,逐台新建 = 白送 draw call)。

| 件 | level 条目 | 补的结构 | 与谁咬合 |
|---|---|---|---|
| 回程踏台 A | `{5905,560,60,14} oneWay` | 下弦梁 5905–5965 @ y574–581;三角节点板 (5960,581)→(5903,581)→(5903,616);角钢斜杆 + 两端螺栓;墙面锚板 5881–5903 @ y601–616;台面黄黑带 y558–563;台底垂影 5899–5971 @ y581–611 | **anchorX=5900** = R-A/R-B 门框墙墩东面(level `partition{5856,470,44,310}` 就在那) |
| 回程踏台 B | `{5905,630,60,14} oneWay` | 同上整体 +70(梁 y644–651,锚板 y671–686) | 同上 |
| 上行阶梯 E2 | `{6200,442,100,22} oneWay` | 梁 y464–471;托架 1 组 tip 6286 → foot 6240 @ y517;锚板 6218–6240 | 大厅后墙锚板(与回廊托架同语言) |
| 上行阶梯 E1 | `{6330,526,100,22} oneWay` | 梁 y548–555;tip 6416 → foot 6370 @ y601;锚板 6348–6370 | 同上 |
| **高台** | `{6480,610,140,90} prop:prop_platform` | 台面=烘焙桁架 6480–6620 @ y610–632;台身腹板 6485–6615 @ y632–692(板缝每 24)+ 两侧角柱 5 宽 + 底裙基座板 6476–6624 @ y691–700 + 地脚螺栓每 30;接地投影椭圆 (6550,701) 166×11;台面黄黑带 y608–613 | 底面 700 = R-B 地面顶面,**原本就坐在地上** |

- **"高台贴涡轮墙→牛腿三角肋"这条不适用**:高台在 6480–6620,涡轮风道墙在 6810/6877,中间隔着 190px,且高台底面 700 正好坐在大厅地面上 —— 它不是悬挑件。因此按"封闭台座"做(腹板/角柱/底裙/地脚),**台身画满整个碰撞盒**(所见即所碰:这一整块能站能挡,不是开放支腿)。牛腿语言改用在**门框落差处**(见 ③/#15),那里才是真悬挑。
- **二层回廊 `{6310,380,310,22}` 明确跳过**(地图批已给主梁 + 3 组斜撑 + 扶手),代码里按 `x/w/y` 三重条件排除,不重复。
- R-A 检修口踏台 `{5545,504,60,22}` 未加托架:它嵌在甲板厚度切口里(甲板 470–540),两侧已有 `deckXsec` 断面承接,不属"悬空黑板"。

### ② R-B 走道下方机械带
`_drawPowerDetail` 尾部新增,depth 0.6(背景之上、玩法层之下),**逐元素 `inR` 越界守卫**,渐暗只铺 `R.x..R.x+R.w`(不按整幅世界宽/高,遵守扩图越界遮罩教训)。

| 元素 | 坐标 | 说明 |
|---|---|---|
| 电缆桥架 ×5 段 | sx = 5940/6260/6580/6900/7220,每段 260×15 @ y740–755 | 托盘上翻边受光 + 13px 一道横档 + 盘内双色电缆;**吊架 3 根/段**(sx+26/+130/+234)从 y700 吊到 740 = 吊在甲板下,不是浮着。sx=7540 那段被 `inR` 拦下(右缘 7800 > 7760)✓ 守卫生效 |
| 管束下行 ×5 处 | x = 6040/6420/6760/7120/7460,每处两根(w17@+0、w11@+24),y700–950 | 横向双段渐变=圆柱明暗(禁平涂);卡箍 y788 / y892 |
| 检修格栅暗门 ×1 | 7130–7290 @ y792–888 | 百叶 8 道 + 四角螺栓 + 底沿黄黑封条;纯视觉。选址避开风道下墙(6810–6990)、补给间(5975–6155)、R-C 封盖(7596–7748) |
| 渐暗 | 背景层 y716–960 alpha **0.5→0.85**(depth 0.58);机械件层 y760–960 alpha 0.12→0.6(depth 0.63) | 分两层:背景按规格沉下去,新加的桥架/管束只吃一层轻的 —— 否则刚补的结构当场被自己的暗角糊掉 |

### ③ 三处门框柱精细化
`_drawThreshold`(x=4600、x=5900 两处;doorTop=240,walkY=470,wallW=92)。**"蜂巢侧同构的那一处"我处理成了 `p.partition` 分支的贴图密度修**(见 §5 附加项),因为蜂巢侧不是 threshold 而是 level 实体。

| 规格条目 | 落地 |
|---|---|
| ①柱面纵向板缝 + 高光棱(受光面朝大厅灯光方向) | 柱体 `px±13 @ y260–470`,横向渐变;**受光侧由两区 tint 明度实算**(`lum = R·0.3+G·0.6+B·0.1`):走廊 149.2 < R-A 192.3 < R-B 205.2 ⇒ 两处 `lit=+1`(受光面朝东,即朝更亮的一侧)。受光棱 `px+8±1.5` 宽 3 |
| ②铆钉沿板缝排、间距 44 | 板缝在 `px±4.5`(宽 1.6)+ 缝右 1px 起翘高光;铆钉**成对**落在 `px±7.5`,y 从 282 起每 **44** 一排(282/326/370/414/458),与墙板模数一致(旧版是柱心单列、间距 64) |
| ③门楣下投影加深梯度 | 由单段 0.55→0 改**两段**:0.68→0.26 @ y260–286(近楣浓而短)、0.26→0 @ y286–360(远楣淡而长) |
| ④柱脚基座板 + 地脚螺栓 | 基座板 `px−16..px+16 @ y454–466`(带受光棱 + 描边),地脚螺栓 `px±10 @ y462`(暗底 + 高光点) |
| ⑤楣上警示灯罩画出灯具结构 | 支架双腿 (x−11 / x+8) @ y228–240 → 灯罩壳 x±16 @ y216–228(罩顶受光棱 + 描边)→ 罩沿 x±13 @ y227–230 → **罩下灯管** rect 22×3.4 @ y227.5(独立 tween 0.6→0.98)→ 软光晕 px_glow @ y231(tween 0.22→0.55)。旧版是一团悬浮的 px_glow |

### ④ 淡化窄条预烘焙(性能账)
- 新增 `_bakeFade(...)`:create 时把"新区图 × 阶梯 alpha"的 9 条窄条烙进一张 `DynamicTexture`,场上每区界只留 **1 个静态 image**。
- **像素等价论证**:`DynamicTexture.repeat()` 内部用的就是一只 TileSprite(`TextureManager.resetTileSprite` → 同一渲染节点),因此同 `tileScale`、同 `tilePosition`、同 tint、同 alpha 时输出与线上路径逐像素相同;条间 1px 重叠的 alpha 合成在纹理里与在屏上等价(两者都是 `a₂c+(1−a₂)(a₁c+(1−a₁)bg)`)。
- **1:1 分辨率**:DT = `190 × 275`(宽=fade 190,高=两区墙带纵向交集 274.17 上取整),**不做任何压缩**。
- **相位对齐**(断言实测通过):`tilePositionX = (sx − R.x)/kx`、`tilePositionY = (y0 − R.top)/ky = 212.520`;阶梯 alpha 序列与旧式 `((i+1)/(n+1))^1.6` 完全一致 → `0.0251, 0.0761, 0.1457, 0.2308, 0.3299, 0.4416, 0.5651, 0.6998, 0.8449`。
- **WebGL1 / 回退不崩**:能力检测语义看齐 `FluidFx.ok` —— 先判 `renderer.type ∈ {WEBGL, CANVAS}`,再 `try/catch` 包住建纹理与 `render()`;任一步不成立就 `_dtOk=false` 并**原样回落 9 条 tileSprite 路径**(两条路径的参数完全同源,回落后观感等同旧版)。同一 `_dtOk` 也保护桁架烘焙。
- **场景重启安全**:烘焙前 `textures.exists(key) → textures.remove(key)`,不复用旧纹理(`addDynamicTexture` 撞 key 会返回 null)。
- 开销对比见 §4。**注意**:视觉与"现状"的唯一差异来自 #13 的纵向交集裁剪(那是本批要修的 bug,不是等价性破坏),见下。

### ⑤ 蜂巢入口悬空复核 → **复核无议**
我对地表 x2450–3400 全部 `platforms(prop)/decor/interactables/checkpoints/breakables` 做了底边支撑面审计(脚本化,不靠目测),31 件里 **28 件底边精确落在 ground/slab/oneWay/prop 顶面上**;另 3 件是设计上的挂件不是悬空:
- `bg_cable_tray` ×2 @ y1394:顶边 1368,吊在 B3 楼板底(1366)下 2px = 吊顶电缆桥架;
- `bg_alarm` @ (3075,1160):贴**后墙**的报警盒(decor 的定义就是"立于后带或贴后墙"),与隔断 `{3095,1076,15,164}` 相隔 9px 属正常前后关系。

`console_hatch`(x3040)/`cp2`(x2480)的 `lift=10 + 接地影` 是 **2026-07-22 用户定版**的"立于走道后沿"读法,不是悬空 —— 未动。
**唯一真悬空的东西是 #21 那两块黑矩形,已在本批修掉。**

---

## 3. 判真视觉条目 #10–#21 / #23 逐条

> 与 spec 五项重叠的,归并处理并在此注明。

### #10 R-B 平铺换行硬缝(world 7736.5)—— 防线级
- `_drawRegions` 新增**区宽吸收余量**:`n = round(R.w/dispW)`,余量 ≤6% 时把 `kx` 改成 `R.w/(n·图宽)`,换行线正好落在区界 ⇒ 缝消失。
  - R-B:dispW=1836.44,n=1,fitK=1.0128 → **1.28% 横向微拉**,`kx` 由 1.03520 → **1.04848**;**`ky` 一字未动**(walkR 构造性对齐与人体尺度不受影响)。断言实测:换行线 n=1.000000 ⇒ 落在区界。
  - **未按 #11 的建议把 `REGIONS.power.w` 放到 1900**:1860 时余量仅 1.28%,放到 1900 会变成 3.46% 拉伸;而"图止 7760 / 墙起 7760"的双硬边已由 #11 的墙面上贴图解决(墙成了合法结构件)。
- R-A 余量 18.5% 超阈值(拉伸会毁掉尺度),改走项目既有 idiom **"接缝藏进结构里"**:在每条换行线压一根 `dev_wall_col` 立管。
  - 立管落点断言实测 **5148.3 / 5696.7**(与判真书里像素实测的两条缝一致),宽 46(@0.5 原生密度正好一根截面柱)、y 280–468(只占墙带,不伸进走道/坑口),含暗底 + 上下法兰 + 柱侧投影,tint 取区色。

### #11 世界边界墙灰盒(x7760)
- 新增 `else if (p.wall)` 分支:不透明暗底 `0x0a0e14` 打底 → `dev_hivewall` 竖向平铺(宽度贴合墙厚 `k=(p.w·2)/图宽`,纵向同比不变形,与 hivewall 同构)→ tint 取 `_regionTintAt(7780)`(新增 helper:区内取区色、区外取最近区,此处 = R-B `0xd6cdb4`)→ 朝内受光棱 3px + 12px 暗过渡(读作端柱,不是一刀切)。
- 兜底 `else` 分支保持不变(留给将来真正没分类的实体)。

### #12 / #23 见 §1。

### #13 淡化带把整幅新区墙铺到上一区头上("天花板上面透出另一个房间")—— 防线级
- 窄条/烘焙一律改按**两区墙带的纵向交集**绘制:`y0 = max(R.top, prevTop)`、`y1 = min(R.top+dispH, prevBot)`,`h≤0` 则不画;`tilePositionY = (y0−R.top)/ky` 保相位。
- `_drawRegions` 里逐区滚动记录 `prevTop/prevBot`,最西区的"上一区"= 旧走廊图整幅(为此给 `bgMeta` 补了 `hDisp` 字段)。
- 实测:R-B 的带由 60–978.2 收到 **280–554.17**(= 与 R-A 带的交集),y<280 的空区不再浮出涡轮区灯罩/弯头管。R-A 侧交集恰为它自己的带(走廊图 −16..659.3 完全包住),观感不变。
- 这是 REGIONS "加一行即开新区" 的通用管线,R-C 带高一旦不同会原样复发,所以做成通用式而不是对 R-B `setCrop` 单点。
- **归并**:与 spec ④ 同一段代码,烘焙纹理高度直接取交集高。

### #14 R-A 墙横带压缩糊化 —— **只出方案,未动手**(见 §6 全文)

### #15 门框墙下基座悬在半空 —— 与 spec ①③ 归并
- 新增 `_drawThresholdFooting(g, x, wallW, walkY, regionWalkY)`,**高度由落差推导,不写死 60**:
  - 平地区(x=4600,`regionWalkY == walkY`):仍是短基座 60px + 底部渐隐,顺带补了与柱脚同光向的受光棱。
  - **落差区(x=5900,walkY=470 / R-B 地面 700)**:按"真机械"重做 ——
    ① 甲板端头断面 5848–5952 @ y470–540(顶板 7 + 腹板加强肋每 19 + 下翼缘 9,与 `_drawUnderdeck` 同一套断面语言);
    ② 承重墙墩 5854–5904 @ y540–700(横向渐变 + 受光棱 + 分段横缝&螺栓**模数 44**),正对 level 的 `partition{5856,470,44,310}`,两者读作同一根墩;
    ③ **悬挑端斜撑牛腿**:节点板三角 (5950,540)→(5903,540)→(5903,606) + 角钢斜杆 + 两端螺栓;
    ④ 墩脚基座板 5808–5874 @ y685–700 + 地脚螺栓 + 接地投影椭圆 (5866,701) 108×11。
- 判真书里"partition 在截图里根本没出现"一条**需更正**:`{5856,470,44,310,partition}` 是**地图批(cd00469)之后**才加进 level 的,而那两张截图早于地图批 —— 不是渲染失败。它现在会正常显示(并且本批把它的贴图密度也修了,见 §5)。

### #16 旧区平铺周期可辨 —— 本批缓解 + 完整解留美术批
- **本批缓解 = 奇数片水平镜像**(负 scaleX,非 `setFlipX`,理由见下):
  - 地标序列由 A A A A 变成 **A A′ A A′**,同一批主角级元素不再以同姿态、同屏幕位置复现;
  - 附带收益:相邻两片在接缝处**像素连续**(镜像轴),1199.76 / 2399.51 / 3599.27 三条平铺硬缝一并消失。
  - **竖向不做 ±20~30px 微偏**(判真书 fix_sketch 里提到的那条):走道面必须恒对齐地面 470(A2 构造性对齐铁律),竖偏会直接毁掉地板线 —— 这条我判定为不可采纳,理由记在此。
  - **为什么用负 scaleX 不用 `setFlipX`**:Phaser 的 `flipX + setCrop` 会把裁切窗与纹理列**一起**镜像(`Frame.setCropUVs` 的 `ox = cutX + (cw − x − width)`),末片会取到"另一端"的纹理列;负 scaleX 走普通裁切分支。末片改用 `setCrop(图宽 − cw, 0, cw, 图高)`。断言实测:末片世界左缘 3599.267 / 右缘 4600.000,取到的纹理列 277.4–1672.0(= 镜像后正对片首的那一段)✓。
  - `_decorateBackdrop` 同步加 `flip` 形参:`X(sx) = bx + (flip ? 图宽−sx : sx)·S`,并新增 `XL(a,b)` 取世界左缘、`inside(a,b)` 取世界右缘 —— 否则动效层(气泡/扫描线/雷达/柱状图/红灯/顶灯带)会与镜像后的图脱钩,重演 07-28"容器不在了泡泡还在冒"的同族事故。
- **完整解留美术批**:按 REGIONS 档案表的做法给 0–4600 排 2–3 张不同概念图轮换,`_decorateBackdrop` 按片的 tex 取对应动效坐标表。本批未做(要出图)。

### #17 decor 非等比拉伸(墙板类被压扁 / cable_tray 反向拉伸)—— 双层修
- **防线级**:decor 渲染处加开发期断言 —— `|kx/ky − 1| > 0.1` 时 `console.warn` 报出图名、实际 kx/ky 与畸变系数,今后任何 decor 条目不能再静默变形。
- **单点级**:畸变 >15% 的一律改"**高度定比例 + 横向 2x 平铺填满槽宽**"(`tileSprite` + `tileScale(ky,ky)`),槽宽 `d.w` 不变。**不按 w 或 h 单边反推**(判真书已算过:office_glass 按 h 会撑到 515px 越过 x3095 隔断、按 w 会矮到 84px)。
- **相位续排**:新增 `decorRun` 表,同图相邻装饰(右缘/左缘相差 ≤2px)接着上一片的相位铺 —— 办公玻璃片A(2655–2925,相位 0→纹理列 0..540)与片B(2925–3095,相位 540→列 540..880)现在是**连续的一面墙**,消掉了"同一盆栽/同一门板各重复一次"的穿帮。
- 实测走平铺路径的 **12 / 24 条**:office_glass ×2、cryo_wall ×2、lab_window、monitor_wall、gunrack、cable_tray ×5。`prop_*` 家具(畸变 ≤12%)保持 `setDisplaySize` 不动。

### #18 单向平台端头假接缝 —— 在本文件内解决,不需要新切件
- **实测切分点**(逐列 RGBA 平均差全搜索 a∈26–42 × b∈238–252):中段取纹理列 **[37, 243)**,首尾相接的列差 **3.21**,**低于图内相邻列平均差 7.62** ⇒ 平铺处比图里任何一处相邻列都更连续 = 无可见接缝。端盖 37 / 36 源px(= 18.5 / 18 世界px)。写进模块常量 `TRUSS`,标注"实测勿手调"。
- **实现**:`_trussMid()` 先烘一张 206×44 的无缝中段;`_trussTex(w,h)` 再按 (w,h) 把"左盖 + 中段平铺 + 右盖"烘成**一整条**纹理(2x 密度,DT = `w·2 × h·2`),同尺寸只烘一次并缓存;`_drawTruss(p)` 场上只创建 **1 个 image** + `setDisplaySize(p.w, p.h)`。
  - 断言实测(300×24 平台):纹理 `truss_600x48`,三段 = 左盖 [0,37)、中段 [37,564)、右盖 [564,600),右盖取纹理列 243–279,三段纵向都拉满 48;显示盒 = 碰撞盒。
  - **密度与旧版完全一致**(横向 0.5 世界px/纹理px = 2x 铁律,纵向按平台高铺满),**碰撞盒不变**。
  - 短台同样受益:`{5905,560,60,14}` 走 `truss_120x28`,**不再"纹理从中截断、右端盖整块丢失"**。
  - 退化路径:烘焙不可用或 `p.w < 44.5` 时整件 `setDisplaySize` —— 宁可整件微缩,也不要半截平台。
- **附带项**:`{6480,610,140,90} prop:prop_platform` 原来把 22px 高的平台贴图纵向拉 4 倍,已改为"烘焙桁架台面(22 高)+ `_drawRiserBody` 台身",见 spec ①。
- 若将来美术批真的切出 capL/mid/capR 三件,`_trussTex` 只需换 3 个源 key,结构不动。

### #19 房间色温硬色阶(world 2399.5)
- 底片一律中性 `0x9096a0`;房间冷色改由**同一张图的第二份拷贝 + 四角 alpha 横向渐变**叠加(Phaser 4 `setAlpha(tl,tr,bl,br)` 逐顶点插值),色温边界与瓦片边界彻底解耦。
- 斜坡 `[2340, 2560]`(中心 2450 = 设计线,右端正好收在门 `d_in` x2560);东界由 4505 对齐到 **REGION_X0 = 4600**(那里本就换区换图,无缝)。
- 只有斜坡跨过的两片需要叠加(实测 片1 alpha 0→0.271、片2 0.271→1),**共 +2 个 image**;片3 整片在房间内直接上冷 tint(与旧版逐像素同色),片0 纯中性。
- **镜像片的四角顺序要反**:`setAlpha` 的 TL/BL 对应**局部左**,负 scaleX 后局部左落在世界右 —— 代码里按 `flip` 交换,已断言。
- 判真书里"东端 4505→4600 那半条"本就无可见台阶,只是名义范围,顺手对齐。

### #20 旧区地表下纯色死块 + 盖掉概念图机械带
- `_drawHiveBackdrop` 的兜底填充起点 **540 → 618**:`bg_corridor` 在源图 y775→880(= world 540→615)仍有百叶格栅/控制箱/管束/红指示灯,实测 world ≥622 才真正纯黑,618 = 零损失把 75 世界px 的"走道下机械带"还回来(直接兑现 scene-fx SKILL:12「地面不画盖板」)。
- 填充本身叠一层 **alpha 0.16 的竖向岩层纹理**(复用 `dev_shaftwall`,0.5 原生密度,depth 0.21),消掉"一个值不变"的死色块;depth 卡在蜂巢分层美术(0.22+)之下,只在 x 0–2645 与 4415–4600 露出,不盖分层图。
- 判真书里被驳回的两条(x=2600 直角边其实是 `hivewall` 侧棱、x≈4460 已被交界暗角覆盖)遵照结论**未动**。

### #21 蜂巢井口甲板厚度段两块纯黑矩形
- 主井道 `dev_shaftwall` 起点由 `H.y`(540)上提到 **490**、高度 1090→1140(暗带垫底同步),让已有的井壁件(导轨槽/检修梯/分段面板)接管甲板厚度段 y506–540。depth 0.3 远低于暗门件(5.28–5.62),上提不会盖住井盖/井坑/断面条。
- 照 `_drawUnderdeck` 的 `pitWell` 补井壁棱(depth 0.32):两侧 9px 亮条 3120/3271 @ y490–544、内侧棱线、顶棱 + 井口下背光暗化 0.55→0 @ y492–514,消掉直角硬切。
- `fillRect(3120,490,160,50)` 保留作兜底暗底(判真书 fix_note 要求)。
- **第三条(`Devices.js:134-135` 的 `dev_hatch_slab` 高度 8 → 覆盖 [498..540])超出我的文件范围,未动**,列入 §5 需主控裁定。

---

## 4. 开销对比(烘焙项必写)

用同一套桩驱动 HEAD 版与工作树版的全部绘制路径实测(`cost.mjs`):

| 指标 | HEAD | 本批 | 差 |
|---|---|---|---|
| 显示对象总数 | **349** | **355** | +6 |
| 其中 TileSprite(每帧要算 UV 矩阵) | **60** | **58** | −2 |
| **过渡带内的对象**(两处区界) | **18 个 tileSprite** | **2 个静态 image** | **−16** |
| DynamicTexture 张数 | 0 | 12 | +12 |
| 烘焙纹理 VRAM 合计 | 0 | ≈ **998 KB** | — |

烘焙清单:`fadebake_duct/power` 各 190×275(204 KB 每张)、`prop_platform_mid` 206×44、`truss_*` 9 张(120×28 ~ 620×44)。

**Draw call 估算**:过渡带原本是 18 个共 23×918 的 tileSprite **叠在同一 190px 竖条里**,每帧各做一次 `TexturerTileSprite.run()`(归一化 + translate + scale + setQuad)并各占一条显示列表条目;现在是 2 个普通 quad,**每帧少 16 条显示列表遍历 + 16 次 UV 矩阵运算**,该带内的重叠透明填充率从 9 层降到 1 层(**over-draw 是这段最贵的东西,因为 9 条窄条在同一屏幕区域全部半透明叠加**)。

净 +6 个显示对象来自本批**新增的结构**(接缝立管 ×2、边界墙 ×1、岩层纹理 ×1、井壁棱 ×1、托架三层 ×3、机械带 ×3、冷色罩 ×2、灯管 ×2、高台投影 ×1,减去省下的 16)。这些绝大多数是**静态 Graphics**(几何一次性烘进顶点缓冲,无纹理绑定)且散布全图、极少同屏。

**诚实声明**:我不能验证"FPS 124 → 160"是否达成 —— 禁用浏览器,无法跑 rAF 计数。**这一项必须实机复测**;若未达标,最大的下一块是 `_drawUnderdeck`/`_drawPowerDetail`/门框的 graphics 数量(可再合并到共享 graphics)。

---

## 5. 附加改动(在同一文件、同一失效模式,已顺手修)

1. **`p.partition` 分支禁 `setDisplaySize`**:`dev_wall_col`(99×540)原被压成 44×310 / 15×120 —— 正是 2026-07-28 已记档的"变形灰模"失效模式(门框上楣踩过一次)。改 0.5 原生密度 `tileSprite` + 不透明暗底 + **窗口相位居中在柱心**(`tilePositionX = (99 − p.w·2)/2`),避免只显示切件的半透明边缘区(墙发虚,同批踩过)。影响 6 条 partition:`{2560,0,45,270}`(正好 1:1)、`{5856,470,44,310}`、`{3095,540/786/1076/1366,15,...}` ×4。
2. `bgMeta` 补 `hDisp` 字段(供 #13 的纵向交集用);`roomTintTo` 由 4505 对齐 REGION_X0(该字段全仓无消费者,仅为与代码一致)。

---

## 6. #14 裁切方案全文(**本批不动手**,交美术批 / 主控)

### 6.1 问题复述
`bg_duct.jpg` 1774×887 在 R-A 只显示 **30.9%**(墙带 274.17px),而 R-B 是 103.5%,同屏 **3.35 倍**精细度差。**真正定罪的不是这个比值而是人体尺度**:R-A 绿色应急灯条量在 world y390–395,离走道面(470)只有 **78px**,而角色视觉高 ~117px ⇒ **走廊顶灯挂在人的胸口**;墙梯档距 ~7 world px(≈13cm,真实梯档 25–30cm),整条梯子比机兵还矮。读作模型屋。

### 6.2 采纳路②(纵向裁切母图),不采纳路①
路①(把 `top` 从 280 提到 ~60)要动 `level_slice.json` 的 `ceiling{4700..5800, y280}`,且与"R-A = 全基地最暗最窄"的区域体量意图相反。**路②只改一张 jpg + 一行 walkR,不碰关卡几何。**

### 6.3 关键推导:ky 只由"裁掉的顶部行数 T"决定
走道面在源图第 **614.7** 行(= 0.693 × 887,walkR 实测值)。裁掉顶部 T 行后:

`ky = 墙高 / (614.7 − T) = 190 / (614.7 − T)` —— 与裁后总高无关。

| T(裁掉顶部行数) | 裁后源图高 | 裁后 walkR | ky | 灯条离地(现 78px→) | 梯档(现 7px→) | 单幅显示宽 dispW | 1300 需几张 |
|---|---|---|---|---|---|---|---|
| 0(现状) | 887 | 0.6930 | 0.3091 | 78px | 7.0px | 548.3 | 2.371 |
| 250 | 637 | 0.5725 | 0.5210 | 131px | 11.8px | 924.2 | 1.407 |
| **300** | 587 | 0.5361 | **0.6038** | 152px | 13.7px | 1071.1 | 1.214 |
| **330** | 557 | 0.5111 | **0.6674** | 168px | 15.1px | 1184.0 | 1.098 |
| **350** ★ | 537 | 0.4929 | **0.7178** | **181px** | **16.3px** | **1273.4** | **1.021** |
| 400 | 487 | 0.4408 | 0.8850 | 223px | 20.0px | 1570.0 | 0.828 |

### 6.4 推荐 T = 350(★),理由
1. **ky 0.718 落在健康带**(全场基准 0.36–0.72:蜂巢 0.358/0.426/0.435/0.493、旧走廊 0.718、R-B 1.035),R-A 由低端离群回到基准上沿,不再与 R-B 形成两个极值挨着的断差。
2. **人体尺度全部归位**:梯档 16.3px ≈ 25cm(真实值)✓;灯条离地 181px,而墙带本身只有 190px ⇒ 灯条落在**离天花板 9px** 处 = 名副其实的顶灯带 ✓(现状 78px 的"胸口灯"是纯粹的错误)。
3. **顺带自动消灭 R-A 的换行缝**:dispW = 1273.4,`1300/1273.4 = 1.021` ⇒ 落进本批新写的 6% 吸收阈值,`kx` 微拉 2.1% 后换行线正好落在区界,**本批加的两根接缝立管届时可以撤掉**(留着也无害,当结构装饰)。
4. 裁后可见内容 = 源图第 350–614.7 行(墙带),614.7–887 行仍照旧溢出到走道面之下作机械带 —— 与 A2 构造性对齐公式完全一致,不需要改公式。

若嫌 T=350 顶灯太高,退而取 T=300–330(ky 0.60–0.67);代价是 R-A 换行缝仍在(1.214 / 1.098 均超阈值),需保留立管,或**再横向裁一刀**:`imgW' = 650/ky` ⇒ T=300 裁到 1077px、T=330 裁到 974px,即可 n=2 整数张。

### 6.5 执行步骤(交美术批)
1. `sharp` 裁 `public/assets/img/bg_duct.jpg`:`extract({ left:0, top:350, width:1774, height:537 })`,quality 84 另存(注意 4399 包体)。
2. **walkR 必须用 `tools/probe-walkline.mjs` + `tools/probe-band.mjs` 在裁后图上重测**,禁止用 6.3 表里的计算值直接填 —— A2 节铁律"walkR 一律实测禁目测"。把实测值写进 `ArenaScene.REGIONS[duct].walkR`。
3. 起 dev server,在 R-A 走廊里把玩家(视觉高 117)停在梯子/灯条旁截一张图做**人体比对**,确认梯档、灯具、栏杆读得出人体尺度。
4. 顺手把 R-A 顶上那块死黑(world < 280,约占一屏 22%)用**一条顶棚/管束带**补掉 —— 这块 T 再大也补不上(墙带高度由 level 的 `ceiling y280` 定死),需要单出一件顶棚切件。
5. **建议一并写进 scene-fx skill A2 节的出区门禁**:新区加行后必须核 `ky ∈ [0.5, 1.1]`,并拿玩家身高比对图内梯档/灯具/栏杆 —— 否则 R-C/R-D "加一行 + 一张概念图" 时同一个坑原样复发。

---

## 7. 跳过项 / 需主控裁定 / decor 清单

### 7.1 明确跳过(附理由)
| 项 | 理由 |
|---|---|
| #14 动手 | 主控点名"先写方案";完整解要重出/重裁母图,本批不出图 |
| #16 完整解(旧区 2–3 张概念图轮换) | 要出图,属美术批;本批已给镜像交替的缓解 |
| #16 的"每片 ±20–30px 竖向微偏" | **判定为不可采纳**:走道面必须恒对齐地面 470(A2 构造性对齐铁律),竖偏直接毁地板线 |
| `REGIONS.power.w` 1860→1900 | 会把 #10 的吸收余量从 1.28% 抬到 3.46%;双硬边已由 #11 的墙面贴图解决 |
| 甲板下两舱(地沟/储藏舱)出图 | exec-map §6.4 第 5 条:本批维持灰盒,不出图 |
| `prop_rack`(畸变 12%)改平铺 | 单件家具不是可平铺墙板,平铺会读作"半个货架";畸变 <15% 阈值,仅 DEV 告警 |

### 7.2 需主控裁定(必须动别的文件才成立,我没动手)
1. **`src/systems/Devices.js:134-135`** —— `dev_hatch_slab` 的 `setDisplaySize(leafW+2, 8)` 只有 8px 高,而 level-devices SKILL:89(暗门 v4 定版)要求"闭合时它封住井道断面 [486..544]"。本批已用井壁件把 y506–540 接上,**但"闭合态断面条封住断面"这条定版仍未兑现**,需把高度改到约 42 并下移中心。超出我的文件范围。
2. **`scene-fx-pipeline/SKILL.md:23` 与 `开发日志.md:50`** 记载的"scrollFactor 1.09 不至于漂出位"是错的(#12 判真书已坐实),应订正为"scrollFactor 必须配基准锚补偿";同时把 `fgAnchor` 写进 A2 节。
3. **`bg_duct` 裁切**(§6)—— 要动 `public/assets/img/` 与 `REGIONS.walkR`。
4. **旧区背景多图轮换**(#16 完整解)—— 要出图。

### 7.3 level/decor 数据清单(**我没改 config**,交主控决定)
| # | 条目 | 现状 | 建议 |
|---|---|---|---|
| 1 | `decor bg_gunrack {4150,1340,160,110}` | 源图 787×240;按原生比例只露 44% 的枪架 | 若要露完整枪架,槽宽应放到 ~360;或让美术切一件窄版 |
| 2 | `decor bg_cryo_wall` @2975 与 @3830 | 两处不相邻 ⇒ 相位都从 0 起,内容完全相同 | 给其中一条加个相位/偏移字段,或把 x 错开半个单元 |
| 3 | `decor prop_rack {…,42,120}` ×3 | 源 75×240,kx/ky = 1.120(轻微横向拉伸) | 宽改 **45** 即精确 1:1 |
| 4 | `decor bg_cable_tray` @5250(160×24)/@5620(200×22) | ky = 0.462 / 0.423,非 0.5 原生密度 | 高统一改 **26**(= 52×0.5) |
| 5 | `decor bg_cable_tray` @2800/@3030/@3420,y1394 | 顶边 1368,离 B3 楼板底(1366)差 2px | 建议 y 改 1392,让它真正贴住楼板底 |
| 6 | `platforms {6480,610,140,90} prop:prop_platform` | 用桁架件当 90 高的实心台 | 本批已按"台面+台身"渲染;若将来出"台座"切件,可换 `prop` 名 |

---

## 8. 实机验收清单(主控做)

1. **机位 06/07/08/09/10/11 重截**:确认 R-B 大厅出现前景柱、R-A 的管子不再跑进走廊、前景管读作圆柱不是灰条(#12/#23)。
2. 走到世界东端 x≈7760:确认(a) 7736.5 的墙面换行缝消失(b) 边界墙有贴图不再是灰板(#10/#11)。
3. 站 R-A/R-B 区界(x≈5800–5950):确认(a) 天花板以上不再浮出涡轮区管束(#13)(b) 门楣灯是"灯具"不是浮光点、柱面有板缝铆钉(spec③)(c) 门框墙下是"甲板断面+墙墩+牛腿"一路插到大厅地面(#15)。
4. R-B 大厅低头看走道下方:桥架/管束/检修暗门读不读得出"下面还有一层"(spec②);跳上回程踏台/E1/E2/高台,看托架朝向对不对(spec①)。
5. 旧区从 x0 走到 x4400:确认(a) 不再有"这屋子刚才走过"(b) 2399.5 的竖向色阶消失(c) 气泡/扫描线/雷达仍严格锁在玻璃与屏内框里(**镜像片是新风险面,重点看这条**)(#16/#19)。
6. 蜂巢 B1–B3:玻璃隔间接缝处不再"房间突然变窄"、CCTV 屏是横的(#17);地表下不再是一整块死色(#20);关井盖时井口下方是井壁不是黑矩形(#21)。
7. 单向平台(旧区 3 条、蜂巢 3 条、R-A 1 条、R-B 4 条):看有没有"两个端盖背靠背"(#18)。
8. **FPS**:rAF 计数器测过渡带附近与大厅,对照 124 基线(spec④,我无法自测)。
9. **WebGL1 回退**:若手上有能强制 WebGL1 的环境,确认过渡淡化带与平台仍正常(回退路径未实跑过)。
