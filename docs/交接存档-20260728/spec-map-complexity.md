# 执行规格:地图复杂度批(用户点名"不能一直直走,多设计场景元素")

仓库 `C:\Users\surpr\Desktop\TimeWarrior-Game`。你改 **config/level_slice.json** 与 **src/scenes/ArenaScene.js 中最小必要的装置接线**(能用 level 数据+现有 Devices 机制表达的,一律不写新代码)。禁止 git、禁止 dev server、禁止浏览器。动手前必读:`.claude/skills/level-devices-pipeline/SKILL.md` **I0 节+碰撞总原则+定版操作规则**、`.claude/skills/scene-fx-pipeline/SKILL.md` **A2 节**、`config/level_slice.json` 全量、`src/systems/Devices.js`(现有装置类型清单)、ArenaScene REGIONS 表。

## 拍板约束(红线)
- 区域大结构(蜂巢→R-A→R-B→[R-C...])与推进顺序是用户拍板,**不加新区、不改区界**;本批只做**区内**支线与纵深。机关/场景配菜已授权("点名内容是例子不是边界")。
- 数值定版勿动(弹药经济/敌伤)。新增补给点用现有 drops/pickup 机制,量级克制(见下)。
- 净高铁律:任何有顶通道 净高 ≥ 最高通行单位胶囊 + 30(玩家 92→过道≥122;机兵 118 会经过的≥148)。敌人出生 y = 所在行走面 y。检查点勿动。封卷门 seal1 勿动。

## A. R-A 管廊(4640-5900,走道 y470,压顶净高 150)——"直廊"破题
1. **地沟支线(crouch 隧道)**:x 约 5080-5420 段,走道下方开电缆地沟:
   - 入口/出口=走道上两个全高竖井小坑(宽 ~90,坑底比走道低 150,站立/起跳无碍,跳出 150≤满跳159 留意再验),中段=净高 120 的蹲行隧道(蹲姿 52)。
   - 结构剖切可见:坑沿+隧道顶=走道板的断面(剖面语言,参照暗门 v4 先例);沟内背景=电缆束+滴水暗调(用现有装饰管线画法,若需新 _decorate 代码,最小化并遵守 A2 越界守卫)。
   - 沟内放 1 处补给(血包×1+步枪弹×1)+ 1 段蒸汽喷口(SteamVent,或 level 蒸汽条目若已有类型;相位错开给通过窗口)= 风险换补给。
   - 入口坑沿黄黑警示+断栅栏残段(视觉预告"这里能下去")。
   - **机兵巡逻区间与两个坑不得交叠**(机兵不掉坑:patrol 边界离坑沿 ≥40)。
2. **中段岔室(闸门储藏间)**:x 约 5550-5680,走道北侧凹室(背景向内一进,地面同 470):入口一道现有类型闸门(E 开启),内放霰弹弹×1+能量芯×1,天花板挂 1 座壁挂炮塔(现有 Turret,俯角覆盖凹室门口,进门前能听/看到威胁——扇区落点先算,不得打到走道必经点,I0 纪律)。
3. **节奏段落**:管廊每 ~400px 一个视觉事件位(已有蒸汽×2 之外):补 1 处顶部滴凝水管+地面小水渍(纯视觉)、1 处红色警示灯段(灯色从绿→红 80px 段落,提示地沟入口在近旁)。用现有装饰机制,新元素一律带 A2 越界守卫。

## B. R-B 动力大厅(5900-7760,地面 y700,顶 y60)——立体回路
1. **二层检修回廊**:y≈400 一条格栅回廊,x 约 6080-6620,桁架单向平台语言(oneWay,tileSprite 桁架贴图,纵向 tileScale 规范照 skill);**必须带托架**:每 ~180px 一组角钢斜撑连到涡轮墙面(视觉层,画进结构而不是悬浮——参照"贴上去≠做进去"教训)。
   - 上行路径:现有高台 → 一跳(落差 ≤84 阶梯化)→ 回廊西端;回廊东端 → 可跳下到风扇前走道(下落无伤)。
   - 回廊上 1 台机兵(patrol 6120-6560,出生 y=回廊面),高位威胁。
   - 回廊中段挂 1 面**检修开关台**(E 交互,devices:event 复用总控台机制):触发**补给间**门开。
2. **补给间**:大厅西南角 x 约 5960-6070 地面,一道闸门(初始锁,收到上述 event 开)+ 内部 RPG 弹×1+血包×1。玩家动线:进厅看见锁门 → 打上高台上回廊 → 开关 → 折返取货 = 区内小回路,"看见-绕行-回来"三拍。
3. **地面掩体**:大厅地面 6250/6700 两处掩体箱(高 52 规范),给换电站前的火拼供蹲防(现在地面偏秃)。
4. **R-C 预告**:东墙 7760 封端处地面加一道"电缆隧道检修口"封闭舱盖(纯视觉结构件:法兰+把手+黄黑封条),不可交互,叙事伏笔。

## C. 全局落地纪律
- 所有新 solids:先写五行说明书(部件/藏哪/怎么动/剖面/碰到什么)进报告;所见即所碰(画托架不碰撞可以——托架属背景装饰层,但平台本体碰撞=显示盒)。
- 出生/巡逻/扇区全部算落点写进报告;新平台可达性用跳跃数学核(满跳159、可跳上≤84、跳跃顶高>92)。
- 新装饰动效必须逐元素越界守卫(A2)。
- level_slice.json 保持现 schema;新增装置若现 Devices 类型不够用,**优先复用**(闸门/开关/蒸汽/炮塔全有),实在要新代码,最小增量并在报告里单列。
- 蹲行隧道净高 120 vs 站立 92:玩家在隧道内站立会顶头——确认现有碰撞对"顶头"处理安全(prevY 守卫全实体已上线),隧道顶=普通实体即可,勿用 oneWay 当顶。

## 产出
- 改完的 config/level_slice.json + 最小 ArenaScene 接线(若有)。node --check 自查 JS;JSON 用 node -e "JSON.parse(...)" 自查。
- 报告写到 `C:\Users\surpr\AppData\Local\Temp\claude\C--Users-surpr\aff8a199-3b89-4965-abe6-65701f1bb0ab\scratchpad\exec-map-report.md`:五行说明书×每装置/落点计算/可达性数学/新增敌人与补给清单/需主控处理的事项。
