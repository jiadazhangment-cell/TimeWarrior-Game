# 时空战士(暂名) — 4399 H5 横版通关游戏

一人项目,纯 vibe coding:开发者(用户)不写代码,由 Claude Code 全程实现。用户是第一次做游戏,希望在过程中学习——实现重要机制时,用一两句话向用户科普"这是什么、为什么这么做"。

## 内容红线(最高优先级,任何代码/美术/特效不得违反)
- 禁止红色血液、血泊、人体断肢、伤口细节。4399 面向青少年,有游戏因血腥全系下架的先例(《闪客快打》)。
- 敌人两类:
  - **生物类**:不断肢,无体液;死亡 = 瘫倒(ragdoll)后消散为能量光点。
  - **机器类人形**:可断肢——断口处短暂电火花 + 金属零件飞溅;**概率断肢,武器越强概率越高**(概率写在 config,可调);尸体留场,玩家可继续射击尸体补断(同样概率判定)。
- 玩家角色可被击败但不断肢。
- 全部美术原创。风格参考 Plazma Burst 2(深色描边矢量 + 半扁平上色 + 低饱和军事科幻 + 红色激光瞄准线),但禁止拷贝任何现有游戏的素材/造型。

## 制作哲学(用户定版 2026-07-16,与内容红线同级)
- **目标永远是高质量游戏**。"临时凑合"最多存在一轮:同一处被用户点名第二次=方向级错误,立即停下重新对齐,禁止给第三个凑合方案(暗门/电梯三轮返工的教训)。
- **结构真实性**:每个装置先当"真机械"设计——现实里它怎么造、怎么动(门有框有厚度有收纳槽,电梯有顶有壁有绞盘缆绳),再从这个结构派生视觉与碰撞。**禁止"贴图思维"**(拿一张像它的图蒙在碰撞盒上=用户明确否决过的做法)。**执行细则:动手前先写"五行说明书"(部件清单/藏在哪/怎么动/剖面看到什么/碰到什么)并对照入侵者2笔记;哲学之间起冲突(如实体化vs安全禁令)时一句话问用户,不自作主张二选一**(暗门四轮、操作台中空的教训)。
- **所见即所碰**:画成实体的必须能碰、物理实体必须画出来(电梯画了顶就必须跳不穿)。每个新装置的验收必含"跳上去撞一撞,看到的和碰到的是否一致"。
- **立体视觉结构**:场景是微俯视投影,一切物体按同一投影画出**顶面/立面/支撑**三件套并与周围咬合(门框嵌进地板缝、轨道固定在井壁),不悬浮、不贴纸。
- **对标法**:新装置/敌人/手感先看《入侵者2》同类怎么做(本地分析台 tmp-cuts/pb2/ + JPEXS 资源库 ffdec-test/),吃透"为什么好"再动手;它的底座=魔改Box2D全物理世界,我们不复制底座,移植语法(结构/前摇/形体状态/道具武器化)。

## 技术栈(已定,勿擅自更换)
- Phaser 4(装的 4.2.1,锁小版本)+ 内置 Matter.js 物理 + Vite 8。逻辑尽量贴 v3 兼容 API,v4 新特性只用于渲染层。
- **渲染上下文=WebGL2**(main.js 经 config.context 注入,官方支持路径;不可用自动回落 WebGL1)。爆炸=GPU 实时流体(src/systems/FluidFx.js,WebGL1 下回退序列帧);**改 FluidFx 前必读其头注释的五条集成坑与 scene-fx skill 现役爆炸条目**。
- 美术管线(角色,2026-07-13 定案):**AI 生成母本 + 多边形切件**(tools/cut-player.mjs)。关节坐标只在脚本 J 表声明一处,pivot/attach 由同一源点自动换算(构造性对齐,禁止手工测量);部件多边形在关节处留圆帽重叠区防露缝;背景=边界泛洪去除;改完跑 preview 模式人眼校版再 final。armgun=双臂持枪整体(换枪=换整图;四把武器的 armgun 变体=tools/cut-armguns.mjs 切件+rigs.json player.armguns,母本=参考32-34)。SVG 管线(assets/svg/→resvg)仅用于场景/物件/临时素材。
- 音频:jsfxr 程序化生成短音效;BGM 用 CC0 曲目(来源记入 docs/素材许可.md)。
- 字体:只允许 assets/fonts/ 下的开源字体(思源系等),禁止引用系统字体(中文字体版权风险)。

## 架构规则
- **混合控制范式**:存活角色用手写运动学控制器驱动(惯性滑行、急停前倾等手感参数化),不挂物理刚体;死亡/断肢瞬间才生成 Matter 部件刚体 + 关节接管。禁止用物理引擎直接驱动存活角色移动(会失控)。
- **EventBus + GameState + Constants 三层**:模块间只通过事件总线通信;所有魔法数字进 config/。
- **数据驱动**:关卡、敌人、武器、平衡数值全部 JSON(config/*.json),调平衡不改代码逻辑。
- **输入统一抽象**:玩法代码只读 InputState(左右/跳/瞄准/开火/切枪),键盘、触屏摇杆各自往这层填值;禁止玩法代码直接读 keyboard.isDown。PC 优先开发,但触屏适配能力从架构上保留。
- **性能预算**:目标 60fps。同屏活动断肢碎块设上限(config 可调);碎块走对象池;ragdoll 静止后休眠或烘焙,及时释放物理体。
- **命中框原则(偏袒玩家)**:敌人命中框≥视觉轮廓,玩家命中框≤视觉轮廓;改角色尺寸/新敌人时同步核对 capsule 与视觉高度。
- **美术精细度标杆**:docs/风格参考/参考7-精细度标杆.png。新做或重做模型(人/枪/场景/特效)按该图的细节密度执行,禁止长期停留在简笔雏形。

## 4399 平台约束(发布相关)
- 产物 = 纯静态 HTML5,入口 index.html,zip 打包上传;vite base 保持 './'(相对路径)。
- 禁止 window.open、外链、外部联系方式;资源文件名禁止含 @ 字符。
- 发布版需在 index.html 的 head 引 h5api 脚本(见 index.html 内注释),开发期不引。
- 包体上限官方未公开——总量按"首屏最小 + 分章懒加载"控制,上传前到 open.4399.cn 后台确认。

## 项目自有 Skill(动手前必读,血泪经验固化)
- **改角色美术/切件/骨架/任何动作(跑蹲跪跳退)** → 先读 .claude/skills/character-anim-pipeline(全套定版参数带【勿改】标记、切件管线、验收方法论、坑清单)。
- **改场景/道具/背景动效/灯光枪焰特效/关卡布置** → 先读 .claude/skills/scene-fx-pipeline(出图配方、玩法尺寸规范、光效三要素、坑清单)。
- **改关卡系统/装置(平台/门/交互/存档/激光/封锁战/炮塔/楼梯)、玩家碰撞新能力、尸体物理** → 先读 .claude/skills/level-devices-pipeline(碰撞总原则、定版操作规则、三轮返工级坑清单、验收方法论)。
- 室内美术找灵感 → docs/室内设计元素库.md(88 条调研元素,含出图/切件/光效分工建议)。
- 步态/姿态参数全部是用户逐轮试玩拍板的定版;手感审美类改动必须用户试玩,技术上"更正确"≠"更好看"。
- 动作/美术标杆=《入侵者2》(本地 Ruffle 分析台 tmp-cuts/pb2/);**机制对标先查反编译源码 tmp-cuts/pb2/scripts(980个.as)与 docs/入侵者2武器体系设计表.md**,新动作先逐帧对标再写数学。

## 工作流
- 命令:`npm run dev`(localhost:5173)/ `npm run build`(产物 dist/)。
- 验证闭环:改完非琐碎逻辑,用 chrome-devtools MCP 打开页面截图看画面、读 console、必要时注入 rAF 计数器测 FPS,确认无误再交付。**物理相关的时序验收必须真实 rAF 跑真实秒数(Matter 锁真实时间,泵帧推不动物理);凡"每帧施力/衰减/计数"必须按时间归一化,否则 165Hz 实机与 60fps 泵帧是两个世界**(细则=level-devices skill I 节)。
- "测试通过 ≠ 好玩":涉及手感/难度/爽感的改动,主动请用户上手试玩拍板。
- 文档纪律:重大决策 → docs/项目方向与决策.md;每次工作 → docs/开发日志.md 记一条;新增工具/依赖 → docs/工具与环境.md;引入任何第三方素材 → docs/素材许可.md。
- git:小步提交,中文 commit message。提交历史同时是应对 4399 版权举证的原创性证据链。
- Phaser API 拿不准时,先查 .claude/skills/ 下的 28 份官方 skill(含 physics-matter、v3-to-v4-migration),不要凭记忆猜 API。
